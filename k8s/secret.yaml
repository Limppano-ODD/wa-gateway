apiVersion: v1
kind: Secret
metadata:
  name: wa-gateway-secrets
  namespace: wa-gateway
type: Opaque
data:
  # Base64 encoded values - update these with your actual values
  # echo -n "your-secret-key" | base64
  KEY: ""
  # echo -n "https://your-webhook-url.com" | base64  
  WEBHOOK_BASE_URL: ""
